<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Bienvenido</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen p-4">
    <div class="bg-white p-8 rounded-2xl shadow-lg w-full max-w-4xl">
        <h1 class="text-3xl font-bold text-center text-gray-800 mb-6">Panel de Control</h1>

        {{#username}}
        <p class="text-xl text-center text-gray-700 mb-8">¡Bienvenido, 
            <span class="font-semibold text-blue-600">{{username}}</span>!
        </p>
        {{/username}}
        {{^username}}
        <p class="text-xl text-center text-gray-700 mb-8">¡Bienvenido!</p>
        {{/username}}

        <!-- Tarjetas principales -->
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 mb-8 items-stretch">
            <!-- Tu Perfil -->
            <div class="bg-blue-50 border border-blue-200 p-6 rounded-lg text-center shadow-sm flex flex-col justify-between min-h-[220px]">
                <div>
                    <h2 class="text-2xl font-bold text-blue-700 mb-3 text-balance">Tu Perfil</h2>
                    <p class="text-gray-600 mb-4 text-balance">Gestiona la información de tu cuenta.</p>
                </div>
                <a href="/profile" class="inline-block bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 transition duration-300">Ver Perfil</a>
            </div>

            <!-- Configuraciones -->
            <div class="bg-green-50 border border-green-200 p-6 rounded-lg text-center shadow-sm flex flex-col justify-between min-h-[220px]">
                <div>
                    <h2 class="text-2xl font-bold text-green-700 mb-3 text-balance">Configuraciones</h2>
                    <p class="text-gray-600 mb-4 text-balance">Ajusta las preferencias de tu aplicación.</p>
                </div>
                <a href="/settings" class="inline-block bg-green-600 text-white py-2 px-4 rounded-md hover:bg-green-700 transition duration-300">Configuración</a>
            </div>

            <!-- Profesor -->
            <div class="bg-pink-50 border border-pink-200 p-6 rounded-lg text-center shadow-sm flex flex-col justify-between min-h-[220px]">
                <div>
                    <h2 class="text-2xl font-bold text-pink-500 mb-3 text-balance">Profesor</h2>
                    <p class="text-gray-600 mb-4 text-balance">Agregar un nuevo profesor.</p>
                </div>
                <a href="/teacher/new" class="inline-block bg-pink-600 text-white py-2 px-4 rounded-md hover:bg-pink-700 transition duration-300">Añadir profesor</a>
            </div>
        </div>

        <!-- Listado de Profesores -->
        <div class="bg-yellow-50 border border-yellow-200 p-6 rounded-lg text-center shadow-sm mb-8">
            <h2 class="text-2xl font-bold text-yellow-600 mb-3">Listado de Profesores</h2>
            <p class="text-gray-600 mb-4">Consulta todos los profesores registrados.</p>
            <a href="/teachers"
               class="inline-block bg-yellow-500 text-white py-2 px-4 rounded-md hover:bg-yellow-600 transition duration-300">
                Ver Listado
            </a>
        </div>

        <!-- Botón Cerrar Sesión -->
        <div class="text-center">
            <a href="/logout"
               class="inline-block bg-red-600 text-white py-2 px-6 rounded-md hover:bg-red-700 transition duration-300 text-lg">
                Cerrar Sesión
            </a>
        </div>
    </div>
</body>
</html>