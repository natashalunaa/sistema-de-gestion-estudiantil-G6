<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Formulario Profesor</title>
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
        /* sacar flechitas del input type="number" */
        input::-webkit-outer-spin-button,
        input::-webkit-inner-spin-button {
            -webkit-appearance: none;
            margin: 0;
        }

        /* sacar flechitas del input type="number" */
        input[type=number] {
            -moz-appearance: textfield;
        }

        /* evitar escribir caracteres no numéricos (como e o .) */
        input[type=number]::-webkit-textfield-decoration-container {
            -webkit-user-modify: read-write-plaintext-only;
        }
    </style>
</head>

<body class="bg-gray-100 flex items-center justify-center min-h-screen p-4">
    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
        <h1 class="text-3xl font-bold text-center text-gray-800 mb-6">Datos Personales</h1>

        {{#successMessage}}
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline">{{successMessage}}</span>
        </div>
        {{/successMessage}}

        {{#errorMessage}}
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline">{{errorMessage}}</span>
        </div>
        {{/errorMessage}}

        <form action="/teacher/new" method="post" class="space-y-4">
            <div>
                <label for="dni" class="block text-gray-700 text-sm font-medium mb-1">DNI:</label>
                <input type="number" id="dni" name="dni" value="{{dni}}" min="0" max="999999999" required
                    class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <div>
                <label for="nombre" class="block text-gray-700 text-sm font-medium mb-1">Nombre:</label>
                <input type="text" id="nombre" name="nombre" value="{{nombre}}" required
                       class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <div>
                <label for="apellido" class="block text-gray-700 text-sm font-medium mb-1">Apellido:</label>
                <input type="text" id="apellido" name="apellido" value="{{apellido}}" required
                       class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <div>
                <label for="mail" class="block text-gray-700 text-sm font-medium mb-1">Correo Electrónico:</label>
                <input type="email" id="mail" name="mail" value="{{mail}}" required
                       class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <div>
                <label for="titulo" class="block text-gray-700 text-sm font-medium mb-1">Título:</label>
                <input type="text" id="titulo" name="titulo" value="{{titulo}}" required
                       class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <button type="submit"
                    class="w-full bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
                Guardar Datos
            </button>

            <div class="text-center mt-4">
                <a href="/" class="text-blue-600 hover:underline text-sm block">Volver al inicio</a>
            </div>
        </form>
    </div>
</body>
</html>
