<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listado de Profesores</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 flex items-center justify-center min-h-screen p-4">
    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-3xl">
        <h1 class="text-3xl font-bold text-center text-gray-800 mb-6">Listado de Profesores</h1>

        {{#successMessage}}
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline">{{successMessage}}</span>
        </div>
        {{/successMessage}}

        {{#errorMessage}}
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline">{{errorMessage}}</span>
        </div>
        {{/errorMessage}}

        {{#teachers.size}}
        <div class="overflow-x-auto">
            <table class="min-w-full bg-white border border-gray-300 rounded-lg shadow-sm">
                <thead class="bg-blue-600 text-white">
                    <tr>
                        <th class="py-2 px-4 text-left">DNI</th>
                        <th class="py-2 px-4 text-left">Nombre</th>
                        <th class="py-2 px-4 text-left">Apellido</th>
                        <th class="py-2 px-4 text-left">Correo Electrónico</th>
                        <th class="py-2 px-4 text-left">Título</th>
                    </tr>
                </thead>
                <tbody>
                    {{#teachers}}
                    <tr class="border-b hover:bg-gray-100">
                        <td class="py-2 px-4">{{dni}}</td>
                        <td class="py-2 px-4">{{nombre}}</td>
                        <td class="py-2 px-4">{{apellido}}</td>
                        <td class="py-2 px-4">{{mail}}</td>
                        <td class="py-2 px-4">{{titulo}}</td>
                    </tr>
                    {{/teachers}}
                </tbody>
            </table>
        </div>
        {{/teachers.size}}

        {{^teachers.size}}
            <p class="text-center text-gray-600 mt-4">No hay profesores registrados todavía.</p>
        {{/teachers.size}}

        <div class="text-center mt-6 space-x-4">
            <a href="/dashboard" class="bg-gray-200 hover:bg-gray-300 text-gray-800 py-2 px-4 rounded-md">Volver al inicio</a>
            <a href="/teacher/new" class="bg-blue-600 hover:bg-blue-700 text-white py-2 px-4 rounded-md">Registrar nuevo profesor</a>
        </div>
    </div>
</body>
</html>
